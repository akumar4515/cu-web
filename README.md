# cu-web (Viewer)

Responsive web viewer for discovering and watching live sender devices.

## Environment
Copy `.env.example` to `.env`:

```env
VITE_BACKEND_URL=https://your-render-service.onrender.com
```

## Run
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```
